fastapi==0.112.0
uvicorn[standard]==0.30.5
httpx==0.27.0
beautifulsoup4==4.12.3
pydantic==2.8.2
python-dotenv==1.0.1
pillow==10.4.0
tqdm==4.66.4
# Torch CPU (Linux/macOS); nếu Windows dùng lệnh riêng trong Dockerfile
torch==2.3.1+cpu; sys_platform != 'win32' --find-links https://download.pytorch.org/whl/cpu
torchvision==0.18.1+cpu; sys_platform != 'win32' --find-links https://download.pytorch.org/whl/cpu
# Cài CLIP qua Git (chính chủ)
git+https://github.com/openai/CLIP.git